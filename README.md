# ひより - カレンダーWebアプリ

月間カレンダーで予定を登録・編集・削除できる、Spring Boot製のWebアプリです。予定はH2データベースに保存されます。

## 起動手順

必要環境: Java 21、Maven

```powershell
mvn spring-boot:run
```

起動後、ブラウザで `http://localhost:8080` を開いてください。終了するときは `Ctrl+C` を押します。
